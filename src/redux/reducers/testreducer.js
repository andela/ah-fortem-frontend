/**
 *
 * @param {object} state
 *
 * This is just a test reducer. It will be removed later
 */

const test = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default test;
